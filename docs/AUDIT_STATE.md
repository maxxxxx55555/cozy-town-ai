# AUDIT_STATE — «Пазл из Жизни»
Волна: 9 (баг-фикс) | План: 10 волн (осталась 10: релиз) | Score: **90%** (9/10; AAB ждёт сети)

## Исправленные баги (реальные дефекты)
1. Android: сохранение при сворачивании (`NOTIFICATION_APPLICATION_PAUSED`) — раньше сейв терялся.
2. Долговременная память росла бесконечно → потолок 200 событий, выкидывается наименее важное (анти-DoS).
3. Сплетни дублировались в память NPC каждый раз → дедуп по (текст, день).
4. Speedhack часов: один кадр мог перемотать дни → кламп 600 сек/кадр (сон приложения не ускоряет игру).
5. Сейв из «будущей» версии игры принимался → отклоняется по version.
6. Кнопка «Данные» создавала панель каждый раз → теперь toggle.
7. Дневная цель «поговори» засчитывалась спамом → кулдаун 5 сек.
8. Тесты делили один save.json → изолированные пути (save_unit/save_integration), тесты безопасны при параллельном прогоне.

## Тесты
76 unit + 19 integration, exit 0. Скриншоты стора пересняты.
